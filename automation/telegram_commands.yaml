- alias: Telegram - Reply with menu commands
  initial_state: on
  trigger:
    platform: event
    event_type: telegram_command
    event_data:
      command: '/menu'
  action:
    service: telegram_bot.send_message
    data_template:
      target: "{{ trigger.event.data.user_id }}"
      message: "/devices\n/status see status of system\n
                /help will list all available commands"
      keyboard: ["/info, /control, /help"]

- alias: Telegram - Reply with status commands
  initial_state: on
  trigger:
    platform: event
    event_type: telegram_command
    event_data:
      command: '/info'
  action:
    service: telegram_bot.send_message
    data_template:
      target: "{{ trigger.event.data.user_id }}"
      message: "Show info commands"
      keyboard: ["/system, /temperature, /lights", "/menu"]

- alias: Telegram - Reply with control commands
  initial_state: on
  trigger:
    platform: event
    event_type: telegram_command
    event_data:
      command: '/control'
  action:
    service: telegram_bot.send_message
    data_template:
      target: "{{ trigger.event.data.user_id }}"
      message: "Show control commands"
      keyboard: ["/not available, /not available", "/not available"]

- alias: Telegram - Reply with all available commands
  initial_state: on
  trigger:
    platform: event
    event_type: telegram_command
    event_data:
      command: '/help'
  action:
    service: notify.telegram
    data_template:
      target: "{{ trigger.event.data.user_id }}"
      message: "/control\n
                /info\n
                /lights\n
                /menu\n
                /system\n
                /temperature\n
                "
